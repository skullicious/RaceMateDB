@model IEnumerable<RaceMateDB.Models.RiderModel>

@{
    ViewBag.Title = "ViewRiderEvents";
}

<h2>ViewRiderEvents</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>

            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>

            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            Position
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{

    foreach (var @event in item.Events)
    {
        foreach (var result in item.EventResults)
        {
        <tr>
        <td>
            @Html.DisplayFor(modelItem => @event.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => result.Position)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>

        }

    }
}
</table>
