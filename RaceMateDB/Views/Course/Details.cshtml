@model RaceMateDB.Models.CourseModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>@Model.Name</h4>

 <div>
    <iframe style="width:100%;height:600px;" src="@Model.VeloViewerURL" frameborder="0" scrolling="no"></iframe>
</div>

    
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
